const footer = {
  display: 'flex',
  alignItems: 'center',
  justifyContent: 'center',
  backgroundImage:  'url(https://landscaping.vamtam.com/wp-content/uploads/2016/12/footer.jpg)',
  backgroundSize: 'cover',
  backgroundPosition: 'center',
  backgroundRepeat: 'no-repeat',
  height: 'max-content',
};

const content = {
  display: 'flex',
  flexDirection: 'column',
  alignItems: 'center',
  justifyContent: 'center',
  padding: '2rem 1rem',
  gap: '1.875rem'
};

const contactContainer = {
  textAlign: 'center',
};

const socialContainer = {
  display: 'flex',
  gap: '1rem',
  alignItems: 'center',
  justifyContent: 'center',
};

const contactLink = {
  color: '#2a7d2e',
};

const text = {
  color: '#000',
  marginTop: '1rem'
}

const icon = {
  color: '#2a7d2e',
  fontSize: '2rem',
}

const values = {
  footer,
  content,
  contactContainer,
  socialContainer,
  icon,
  contactLink,
  text
}

export default values;